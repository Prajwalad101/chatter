interface EVENT {
  connection: 'connection';
  disconnect: 'disconnect';
  'chat message': 'chat message';
}

const EVENTS: EVENT = {
  connection: 'connection',
  disconnect: 'disconnect',
  'chat message': 'chat message',
};

export default EVENTS;
